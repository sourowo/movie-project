# GitHub Pages 資料更新方式

`showtimes.json` 不應再被 commit。網站會從 GitHub Release 的固定 asset 讀取資料，
而 GitHub Actions 會在每週三台灣時間 17:00、19:00 各自執行一次爬蟲並覆寫該 asset。

## 第一次部署

1. 將 `index.html`、`requirements.txt`、`.github/workflows/update-showtimes.yml` 推送到
   `sourowo/movie-project`。
2. 如果 `showtimes.json` 曾經被 Git 追蹤，執行下列指令把它從「目前版本」移除；本機檔案會保留。

   ```powershell
   git rm --cached showtimes.json
   git add index.html requirements.txt .github/workflows/update-showtimes.yml .gitignore
   git commit -m "自動更新場次資料且不納入 Git"
   git push
   ```

3. 在 GitHub repository 的 **Actions**，開啟「更新電影場次（不寫入 Git）」並按 **Run workflow** 一次。
   第一次成功後會自動建立 `showtimes-data` Release；前端即可讀到其 JSON asset。

## 注意事項

- `index.html` 的 `DATA_FILE` 已寫死為 `sourowo/movie-project`。若改名或 fork repository，必須更新該 URL。
- Scheduled workflow 在長期完全沒有 repository 活動時，GitHub 可能自動停用；此時到 Actions 頁面手動執行一次即可重新啟用。
- 移除追蹤會停止未來的 JSON commit；舊的 Git 歷史仍保留舊檔案。如要連既有歷史的容量也回收，必須另行以 `git filter-repo` 重寫歷史並 force-push，請先備份並確認所有協作者都同意。
- 爬蟲會逐一查詢全台影城並保留請求間隔，完整執行約需 45 分鐘；workflow 的上限已設為 60 分鐘。
